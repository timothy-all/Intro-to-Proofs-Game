import Game.Levels.RelationWorld.L08_Relations

World "RelationWorld"
Level 9

Title "Order Relations"

Introduction " inprogress All of the standard order relation definitions have been added to your inventory. Give the problem below a shot."

Statement {u : Type} (R: Rel_on u) : (R ∘ R).set ⊆ R.set ↔ isTransitive R := by
  sorry


Conclusion "English proof?"

/- Use these commands to add items to the game's inventory. -/
